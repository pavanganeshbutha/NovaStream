const Logo = () => {
  return (
    <div
      className="text-3xl font-extrabold text-transparent bg-clip-text bg-linear-to-r from-(--color-neon-fuchsia) to-(--color-neon-cyan)"
      style={{ fontFamily: "var(  --font-heading)" }}
    >
      NovaStream
    </div>
  );
};

export default Logo;
